# Shared

## Usage

Install package:

```bash
yarn add @fethcat/shared
```

Use services:

```typescript
import { extractPackageJson } from '@fethcat/shared'

await extractPackageJson()
```
