# Shared

## Usage

Install package:

```bash
pnpm add @fethcat/shared
```

Use services:

```typescript
import { extractPackageJson } from '@fethcat/shared'

await extractPackageJson()
```
